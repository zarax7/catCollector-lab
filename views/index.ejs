<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="./stylesheets/style.css" />
  </head>
  <body>
    <nav>
      <div>
        <a href="/catCollectors/viewCats">View Cats</a>
        <a href="/toyCollectors/viewToys"> View Toys</a>
        <a href="/catCollectors/addCat"> Add a new Cat</a>
        <a href="/toyCollectors/addToy"> Add a new toy</a>
      </div>
    </nav>
    <!-- <ul>
      <% cat.forEach(function(c){ %>
      <li>
        <form
          action="/catCollectors/<%= c.id %>?_method=DELETE"
          method="POST"
          class="delete-form"
        >
          <button type="submit">X</button>
        </form>
        <a href="/catCollectors/<%= c.id %>"> <%= c.name %> </a>
        <form
          class="update-form"
          action="/catCollectors/<%= c.id %>/edit"
          method="GET"
        >
          <button type="submit">Update Cat</button>
        </form>
      </li>
      <% }); %>
    </ul>
    <a href="/catCollectors/new">Add Cat</a> -->
  </body>
</html>
